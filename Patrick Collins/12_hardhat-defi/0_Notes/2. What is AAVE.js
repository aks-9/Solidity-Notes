//* 2. What is AAVE

// A decentralized protocol to lend and borrow money. It treats everything as an ERC20 token.

// Create a new folder called 'scripts' and in it create a new file called 'aaveBorrow.js'.

//* This is 'aaveBorrow.js' file.

//? What we need to perform:
// 1. Deposite collateral programatically: ETH / WETH
// 2. Borrow another asset: DAI
// 3. Repay DAI

async function main() {}

main()
    .then(() => process.exit(0))
    .catch((error) => {
        console.error(error);
        process.exit(1);
    });
